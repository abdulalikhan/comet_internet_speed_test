import 'package:flutter/material.dart';

// Background Color
Color bgCol = Color.fromRGBO(35, 39, 42, 1);

// Progress Bar Colors
Color progressBg = Color.fromRGBO(44, 47, 51, 1);
Color progressFill = Color.fromRGBO(114, 137, 218, 0.8);
Color gaugeRange1 = Color.fromRGBO(153, 170, 181, 1);
Color gaugeRange2 = Color.fromRGBO(114, 137, 218, 1);
Color needleCol = Color.fromRGBO(114, 137, 218, 1);

// Start Test Button
LinearGradient buttonGradient = LinearGradient(
  colors: <Color>[
    Color.fromRGBO(114, 137, 218, 0.7),
    Color.fromRGBO(114, 137, 218, 0.8),
    Color.fromRGBO(114, 137, 218, 1),
  ],
);

// Text Color
Color txtCol = Colors.white;

// Error Message
Color errorBg = Colors.red;
Color errorTxt = Colors.white;
